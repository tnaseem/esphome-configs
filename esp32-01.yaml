substitutions:
  device_name: esp32-01
  device_ip: 192.168.1.22

esphome:
  name: ${device_name}
  friendly_name: ${device_name}

packages:
  base: !include common/base.yaml
  wifi: !include common/wifi.yaml
  role: !include roles/remote-receiver.yaml
